# Retab UI — web app

This is the documentation and registry app for [Retab UI](https://retab.com/ui).

```bash
pnpm install
pnpm v4:dev            # dev server
pnpm v4:registry:build # build the shadcn registry into public/r
```
