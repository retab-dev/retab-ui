{
  "title": "Content Safety Classification Schema",
  "description": "A schema for classifying a message into a single, mutually exclusive content-safety category based on severity.",
  "type": "object",
  "X-SchemaType": "flat",
  "properties": {
    "flag": {
      "type": "string",
      "description": "The single content-safety category that best describes the most severe violation present, or 'None' if no category applies.",
      "enum": [
        "Hate",
        "Harassment",
        "Sexual",
        "Violence",
        "Self-Harm",
        "Illicit Behavior",
        "Spam / Scam",
        "None"
      ]
    }
  },
  "required": ["flag"],
  "additionalProperties": false,
  "$defs": {}
}
