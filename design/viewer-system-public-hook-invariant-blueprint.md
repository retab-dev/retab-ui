# Viewer System Public Hook Invariant Blueprint

This blueprint is superseded by
[`viewer-system-shadcn-platonic-blueprint.md`](./viewer-system-shadcn-platonic-blueprint.md).

The active invariant is:

```txt
public anatomy over public state
private provider context
no broad aggregate hooks
no public part-state hooks without external composition evidence
explicitly internal selectors when files must stay split
```

Use the shadcn platonic blueprint for current implementation decisions.
