{
  "title": "Investor News Topic Tagging Schema",
  "description": "A schema for tagging news snippets with exactly one investor-relevant topic from a fixed set, as required for investor-focused news classification.",
  "type": "object",
  "X-SchemaType": "flat",
  "properties": {
    "topic": {
      "type": "string",
      "description": "The single investor-relevant topic that best captures the primary angle of the news snippet.",
      "enum": [
        "Earnings",
        "M&A",
        "Regulation",
        "Macroeconomic Data",
        "Management Changes",
        "Product Launch",
        "Litigation",
        "Market Rumor"
      ]
    }
  },
  "required": ["topic"],
  "additionalProperties": false,
  "$defs": {}
}
