{
  "title": "Bank Statement",
  "type": "object",
  "description": "Transactions extracted from a business checking account statement.",
  "properties": {
    "transactions": {
      "type": "array",
      "title": "Transactions",
      "description": "Every transaction on the statement, in source order.",
      "items": {
        "type": "object",
        "title": "Transaction",
        "properties": {
          "date": {
            "type": "string",
            "format": "date",
            "title": "Date",
            "description": "Transaction date in ISO-8601 format (YYYY-MM-DD)."
          },
          "amount": {
            "type": "object",
            "title": "Amount",
            "description": "Signed transaction amount with its currency.",
            "properties": {
              "amount": {
                "type": "number",
                "title": "Amount",
                "description": "Transaction amount as printed."
              },
              "iso_4217_currency_code": {
                "type": "string",
                "title": "Currency",
                "description": "ISO 4217 currency code, e.g. USD."
              }
            },
            "required": ["amount", "iso_4217_currency_code"],
            "additionalProperties": false
          },
          "description": {
            "type": "string",
            "title": "Description",
            "description": "Transaction description as printed on the statement."
          },
          "check_number": {
            "type": ["string", "null"],
            "title": "Check Number",
            "description": "Check number when the transaction is a check, otherwise null."
          },
          "transaction_type": {
            "type": "string",
            "enum": ["CREDIT", "DEBIT"],
            "title": "Type",
            "description": "Whether the transaction credits or debits the account."
          },
          "account_number_last4": {
            "type": "string",
            "title": "Account (last 4)",
            "description": "Last four digits of the account number."
          }
        },
        "required": [
          "date",
          "amount",
          "description",
          "check_number",
          "transaction_type",
          "account_number_last4"
        ],
        "additionalProperties": false
      }
    }
  },
  "required": ["transactions"],
  "additionalProperties": false
}
